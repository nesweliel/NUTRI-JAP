name: Build NUTRI V9 APK

on:
  push:
    branches: [ main ]
    paths:
      - 'native-v9/**'
      - '.github/workflows/build-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: native-v9
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace
      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk NUTRI-V9-BLUESTACKS.apk
      - uses: actions/upload-artifact@v4
        with:
          name: NUTRI-V9-BLUESTACKS
          path: native-v9/NUTRI-V9-BLUESTACKS.apk
          if-no-files-found: error
